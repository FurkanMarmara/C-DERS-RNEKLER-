#include <stdio.h>
#include <conio.h>
main()
{
int girilensayi;
printf ("Sayı Giriniz : ");
scanf("%d",&girilensayi);
  if(girilensayi==5) // Burdaki if kelimesi türkçede eğer anlamına gelmektedir Eğer kullanıcının girdiği sayı 5 ise aşağısındaki kodlar 
  //çalışacak
  {
    Printf("Merhaba Dünya");
  }
  getch();
}
